#quartile
x=ceiling(rnorm(100,50,10))
x
quantile(x)
quantile(x,c(0.25,0.5,0.75))
quantile(x,0.75)
IQR(x)
max(x)
min(x)
summary(x)
quantile(x,seq(0.01,1,by=0.01))
quantile(x,seq(0.1,1,by=0.1))
quantile(x)
quantile(x,seq(0.1,0.3,by=0.1))
plot(density(x))
hist(x)
library(timeSeries)
mtcars
colQuantiles(mtcars)
colAvgs(mtcars)
max(mtcars$mpg)
colQuantiles(mtcars,prob=0.1)
colQuantiles(mtcars,prob=0.1,type=1)
colSkewness(mtcars)
colKurtosis(mtcars)
kurtosis(mtcars)
vars=c('mpg','hp','wt')
head(mtcars[vars])
summary(mtcars[vars])
sapply(mtcars[vars],mean)
sapply(mtcars,fivenum)
sapply(mtcars,sd)
sapply(mtcars[vars],median)

update()
library(Hmisc)
describe(mtcars[vars])
library(installr)
updateR()

